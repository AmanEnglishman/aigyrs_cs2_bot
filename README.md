# FACEIT Telegram Bot

Telegram бот для просмотра статистики игроков FACEIT (ELO, K/D, ADR, HS% и другие метрики).

## 🚀 Возможности

- Поиск игроков по никнейму
- Просмотр ELO и уровня
- Статистика: K/D, K/R, ADR, HS%
- Отображение флага страны игрока
- Поддержка CS2

## 📋 Требования

- Python 3.12+
- Docker и Docker Compose (для развертывания)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- FACEIT API Key (получить на [FACEIT Developer Portal](https://developers.faceit.com/))

## 🛠 Установка и запуск

### Локальная разработка

1. Клонируй репозиторий:
```bash
git clone <repository-url>
cd aigyrs
```

2. Создай виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установи зависимости:
```bash
pip install -r requirements.txt
```

4. Создай файл `.env`:
```bash
cp .env.example .env
# Отредактируй .env и добавь свои токены
```

5. Запусти бота:
```bash
python bot.py
```

### Docker развертывание

1. Создай файл `.env`:
```bash
cp .env.example .env
# Отредактируй .env
```

2. Собери и запусти:
```bash
docker compose up -d --build
```

3. Просмотр логов:
```bash
docker compose logs -f faceit-bot
```

4. Остановка:
```bash
docker compose down
```

### Использование Makefile

```bash
make build      # Собрать Docker образ
make up         # Запустить контейнер
make down       # Остановить контейнер
make logs       # Просмотр логов
make restart    # Перезапустить контейнер
make shell      # Войти в контейнер
```

## 📱 Команды бота

- `/start` — приветствие и краткая информация
- `/help` — список доступных команд
- `/faceit <ник>` — показать статистику игрока (ELO, K/D, ADR, HS%)
- `/faceit_maps <ник>` — статистика по картам
- `/faceit_matches <ник>` — последние матчи игрока

### Inline-кнопки

После запроса профиля игрока появляются inline-кнопки для быстрого доступа:
- 🗺 **Карты** — статистика по картам
- 🎮 **Матчи** — последние матчи
- 📊 **Профиль** — вернуться к основному профилю

Пример:
```
/faceit Ars_Ki
```

## 🏗 Структура проекта

```
aigyrs/
├── bot.py              # Основной файл бота
├── faceit_client.py    # Клиент для работы с FACEIT API
├── requirements.txt    # Python зависимости
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
├── .dockerignore       # Исключения для Docker
├── .env.example        # Пример файла с переменными окружения
├── Makefile           # Удобные команды
└── README.md          # Документация
```

## 🔧 Конфигурация

Все настройки задаются через переменные окружения в файле `.env`:

- `TELEGRAM_BOT_TOKEN` — токен Telegram бота
- `FACEIT_API_KEY` — API ключ FACEIT

## 📝 Логирование

Бот использует структурированное логирование:
- Уровни: DEBUG, INFO, WARNING, ERROR
- Логи выводятся в stdout (удобно для Docker)

## 🐛 Troubleshooting

### Бот не отвечает
- Проверь, что `TELEGRAM_BOT_TOKEN` правильный
- Убедись, что бот запущен: `docker compose ps`

### Ошибки FACEIT API
- Проверь, что `FACEIT_API_KEY` валидный
- Убедись, что API ключ имеет нужные права

### Проблемы с Docker
- Проверь логи: `docker compose logs faceit-bot`
- Пересобери образ: `docker compose build --no-cache`

## 📄 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются!

